# Acres API contracts

Generated by `npm run contracts:generate`. Keep this matrix aligned with
`openapi.json` and `schema.graphql`; CI runs `contracts:check`.

## REST

| method | canonical path | auth | notes |
| --- | --- | --- | --- |
| GET | /health | public | version-neutral liveness |
| GET | /health/ready | public | version-neutral readiness |
| GET | /metrics | private/ops | version-neutral Prometheus metrics |
| GET | /api/v1/auth/csrf | public | CSRF token issue route |
| POST | /api/v1/auth/register | public + CSRF | creates account/session |
| POST | /api/v1/auth/login | public + CSRF | creates session |
| POST | /api/v1/auth/logout | session + CSRF | revokes session |
| GET | /api/v1/auth/session | optional session | anonymous or active session |
| GET | /api/v1/account | session | safe account profile |
| GET | /api/v1/regions | public | global region list |
| GET | /api/v1/regions/:slug | public | one global region |
| POST | /api/v1/forms/contact | public + CSRF | public contact form |
| GET | /api/v1/jobs/runs | session | recent scheduled jobs |
| GET | /api/v1/organizations | session | account organizations |
| POST | /api/v1/organizations | session + CSRF + Idempotency-Key | create organization |
| GET | /api/v1/organizations/:organizationId | session + organization.read | selected organization |
| PATCH | /api/v1/organizations/:organizationId | session + CSRF + organization.update | rename organization |
| GET | /api/v1/organizations/:organizationId/members | session + members.read | member list |
| PATCH | /api/v1/organizations/:organizationId/members/:membershipId | session + CSRF + members.change_role | change role |
| DELETE | /api/v1/organizations/:organizationId/members/:membershipId | session + CSRF + members.revoke | revoke member |
| POST | /api/v1/organizations/:organizationId/ownership-transfers | session + CSRF + ownership.transfer + Idempotency-Key | transfer ownership |
| GET | /api/v1/organizations/:organizationId/invitations | session + invitations.read | invitation metadata |
| POST | /api/v1/organizations/:organizationId/invitations | session + CSRF + members.invite + Idempotency-Key | issue invitation |
| DELETE | /api/v1/organizations/:organizationId/invitations/:invitationId | session + CSRF + invitations.revoke | revoke invitation |
| POST | /api/v1/invitations/accept | session + CSRF + Idempotency-Key | accept invitation |
| GET | /api/v1/datasets | session + datasets.read | organization dataset list |
| POST | /api/v1/datasets | session + CSRF + datasets.create + Idempotency-Key | create dataset metadata |
| GET | /api/v1/datasets/:datasetId | session + datasets.read | one organization dataset |
| PATCH | /api/v1/datasets/:datasetId | session + CSRF + datasets.update | update dataset metadata |
| GET | /api/v1/datasets/:datasetId/versions | session + datasets.read | immutable published versions |
| POST | /api/v1/datasets/:datasetId/mappings | session + CSRF + ingestion.run + Idempotency-Key | create column mapping |
| POST | /api/v1/datasets/:datasetId/ingestion-runs | session + CSRF + ingestion.run + Idempotency-Key | queue ingestion run |
| GET | /api/v1/ingestion-runs/:runId | session + ingestion.read | durable run status |
| GET | /api/v1/ingestion-runs/:runId/events | session + ingestion.read | ingestion status SSE stream |
| GET | /api/v1/ingestion-runs/:runId/issues | session + ingestion.read | bounded validation issues |
| DELETE | /api/v1/ingestion-runs/:runId | session + CSRF + ingestion.cancel | cancel before publication |
| GET | /api/v1/analytics/metrics | session + analytics.read | active metric definitions |
| GET | /api/v1/analytics/metrics/:metricId | session + analytics.read | one scoped metric definition |
| GET | /api/v1/analytics/observations | session + analytics.read | bounded observation read model |
| GET | /api/v1/analytics/aggregates | session + analytics.read | deterministic aggregate read model |
| GET | /api/v1/analytics/aggregates/:aggregateId/evidence | session + analytics.read | aggregate evidence lineage |
| GET | /api/v1/reports | session + reports.read | organization report list |
| POST | /api/v1/reports | session + CSRF + reports.create + Idempotency-Key | create draft report and revision |
| GET | /api/v1/reports/:reportId | session + reports.read | report detail with latest revision |
| PATCH | /api/v1/reports/:reportId | session + CSRF + reports.update | update report metadata with expected version |
| POST | /api/v1/reports/:reportId/revisions | session + CSRF + reports.update + Idempotency-Key | create next draft revision |
| PATCH | /api/v1/reports/:reportId/revisions/:revisionId | session + CSRF + reports.update | update draft revision content with expected version |
| POST | /api/v1/reports/:reportId/revisions/:revisionId/publish | session + CSRF + reports.publish + Idempotency-Key | publish immutable revision |
| GET | /api/v1/reports/:reportId/revisions/:revisionId/evidence | session + reports.read | revision evidence links |
| GET | /api/v1/exports | session + exports.read | export request list |
| POST | /api/v1/exports | session + CSRF + exports.create + Idempotency-Key | queue CSV or PDF export |
| GET | /api/v1/exports/:exportId | session + exports.read | export request status |
| GET | /api/v1/exports/:exportId/events | session + exports.read | export status SSE stream |
| GET | /api/v1/exports/:exportId/download | session + exports.read | signed attachment download URL |

Old unversioned product paths are removed in Phase 4 and return 404. `/graphql`
is intentionally unversioned and POST-only.

## GraphQL

| query | permission | notes |
| --- | --- | --- |
| viewer | authenticated member | safe account + active membership |
| organization | organization.read | active organization summary |
| organizationMembers(first, after) | members.read | bounded connection |
| organizationInvitations(first, after) | invitations.read | metadata only, no raw tokens |
| organizationAuditEvents(first, after) | audit.read | bounded event metadata |
| regions(first, after) | authenticated member | global public region connection |
| region(slug) | authenticated member | one global public region |

The schema has no mutation or subscription root in Phase 4.
